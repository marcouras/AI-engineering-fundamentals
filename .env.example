# ── API Keys ─────────────────────────────────────────────────────────────────
# Ottieni la tua chiave su: https://console.anthropic.com
ANTHROPIC_API_KEY=sk-ant-inserisci-la-tua-chiave-qui

# ── Configurazione opzionale ──────────────────────────────────────────────────
# Modello di default (puoi cambiarlo per sperimentare)
# ANTHROPIC_MODEL=claude-haiku-4-5-20251001

# Temperatura di default
# TEMPERATURE=0.7

# ── Lezione 5 — Tool use (opzionale) ─────────────────────────────────────────
# API meteo gratuita: https://open-meteo.com (non richiede chiave)
# API Wikipedia: non richiede chiave
